import React from 'react';
import {Route} from 'react-router';
import {StringRepeating} from './stringRepeating.component';

export default (
  <Route path="string-repeating" component={StringRepeating} />
);
